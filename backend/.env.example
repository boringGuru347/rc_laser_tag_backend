# Rename this file to .env and update values as needed

# MongoDB connection string (include credentials if required)
MONGODB_URI=mongodb://127.0.0.1:27017

# Database and collection
DB_NAME=lasertag
COLLECTION_NAME=students

# Server port
PORT=3000

# Absolute path to the JSON data (or keep relative as from project root)
DATA_JSON_PATH=../contacts_rolls.json
